# Dependencies. Playwright is installed locally for verification runs only;
# the site build is plain node with no dependencies, and committing these
# would make Vercel npm-install Playwright on every deploy.
node_modules/
package.json
package-lock.json

# OS / editor noise
.DS_Store
Thumbs.db
*.swp

# SEO audit workspace. Vercel serves the repo root, so anything committed here
# goes live; keep the audit out of the deploy.
nortonequipmentco.com-audit/

# Finder duplicate artifacts ("index 2.html", "memphis-tn 2/")
* 2.html
* 2/
* 3/
